spring:
  application:
    name: weekly-planner-api-test

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.9

jwt:
  secret: test-secret-key-for-jwt-minimum-32-characters
  expiration: 86400000
